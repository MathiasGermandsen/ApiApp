version: '3.8'
services:
  webapplication1:
    image: myrepo/webapplication1:latest
    ports:
      - "80:80"
    secrets:
      - DefaultConnection
    deploy:
      restart_policy:
        condition: on-failure

secrets:
  DefaultConnection:
    external: true
